import { TotalDiscountPipe } from './total-discount.pipe';

describe('TotalDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
