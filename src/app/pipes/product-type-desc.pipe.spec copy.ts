import { ProductTypeDescPipe } from './product-type-desc.pipe';

describe('ProductTypeDescPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTypeDescPipe();
    expect(pipe).toBeTruthy();
  });
});
